server:
    servlet:
        context-path: /cx

spring:
    jpa:
        show-sql: true
        properties:
            hibernate:
                dialect: org.hibernate.dialect.MySQL5Dialect
        hibernate:
            ddl-auto: update
    datasource:
        url: jdbc:mysql://127.0.0.1:3306/salesdb
        username: appSale
        password: app1001
---

spring:
    config:
        activate:
            on-profile: dev
    datasource:
        url: jdbc:mysql://${MYSQL_HOST}:${MYSQL_PORT}/salesdb
        username: ${MYSQL_USER}
        password: ${MYSQL_PASS}